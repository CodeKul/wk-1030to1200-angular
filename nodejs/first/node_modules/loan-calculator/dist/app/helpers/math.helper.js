"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * This is a class to help with some basic math for arrays
 */
var MathHelper = /** @class */ (function () {
    function MathHelper() {
    }
    /**
     * Returns the mode, most common occurrence, in an array
     * @param {number[]} input the input array
     * @returns {number[]} an array of each of the mode for an array, can be more than one
     */
    MathHelper.mode = function (input) {
        var items = {};
        var mostOccurrences = 0;
        input.forEach(function (item) {
            var key = item.toString();
            if (Object.keys(items).indexOf(key) > -1) {
                items[key] += 1;
            }
            else {
                items[key] = 1;
            }
            if (items[key] > mostOccurrences) {
                mostOccurrences = items[key];
            }
        });
        var returnedList = [];
        Object.keys(items).forEach(function (key) {
            if (items[key] === mostOccurrences) {
                returnedList.push(parseFloat(key));
            }
        });
        return returnedList;
    };
    /**
     * Finds the largest value in an array of numbers
     * @param {number[]} input the input array
     * @returns {number} the largest number in the array
     */
    MathHelper.max = function (input) {
        var max = 0;
        input.forEach(function (val) {
            if (val > max) {
                max = val;
            }
        });
        return max;
    };
    return MathHelper;
}());
exports.MathHelper = MathHelper;
//# sourceMappingURL=math.helper.js.map