/**
 * This is a class to help with some basic math for arrays
 */
export declare class MathHelper {
    /**
     * Returns the mode, most common occurrence, in an array
     * @param {number[]} input the input array
     * @returns {number[]} an array of each of the mode for an array, can be more than one
     */
    static mode(input: number[]): number[];
    /**
     * Finds the largest value in an array of numbers
     * @param {number[]} input the input array
     * @returns {number} the largest number in the array
     */
    static max(input: number[]): number;
}
