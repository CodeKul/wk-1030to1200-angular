/**
 * LowPayEntry Class
 * This is used when defining a low payment month
 */
export declare class LowPayEntry {
    month: number;
    payment: number;
    constructor(month: number, payment: number);
}
