/**
 * AmortizationEntry Class
 * A single entry that represents a single month in an amortization schedule
 */
export declare class AmortizationEntry {
    entry: number;
    interest: number;
    principal: number;
    balance: number;
    payment: number;
    edited: boolean;
    month: number;
    payoff: number;
    constructor(entry: number, interest: number, principal: number, balance: number, payment: number);
}
