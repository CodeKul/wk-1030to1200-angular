/**
 * Loan Class
 * This class is used when initially making a request for loan calculations
 */
import { LowPayEntry } from './low_pay_entry.model';
export declare class Loan {
    amount: number;
    down: number;
    deferred: number;
    term: number;
    rate: number;
    low: LowPayEntry[];
    monthlyRate: number;
    deferredInterest: number;
    constructor(amount: number, down: number, deferred: number, term: number, rate: number, low: LowPayEntry[]);
}
